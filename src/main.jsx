import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
//hooking up our index html with our react code
// this is our js code for react
// we are using react dom to render our app component using the root element (line 6)